$ mkdir ~/.bin
$ PATH=~/.bin:$PATH
$ curl http://commondatastorage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
$ chmod a+x ~/.bin/repo
$ mkdir $WHATEVER-NAME-YOU-CHOOSE
$ cd $WHATEVER-NAME-YOU-CHOOSE
$ repo init -u https://github.com/thefreaxproject/platform_manifest -b master
$ repo sync
