$ mkdir ~/.bin
$ PATH=~/.bin:$PATH
$ curl https://dl-ssl.google.com/dl/googlesource/git-repo/repo > ~/.bin/repo
$ chmod a+x ~/.bin/repo
$ mkdir $WHATEVER-NAME-YOU-CHOOSE
$ cd $WHATEVER-NAME-YOU-CHOOSE
$ repo init -u https://github.com/MoltenMotherBoard/platform -b master
$ repo sync
